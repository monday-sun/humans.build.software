---
import { getCollection } from 'astro:content'

// Get the primary author (Monday)
const authors = await getCollection('authors')
const primaryAuthor = authors.find((author) => author.id === 'monday-romelfanger') || authors[0]

// Redirect to the author's page
return Astro.redirect(`/authors/${primaryAuthor.id}`)
---
